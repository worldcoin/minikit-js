{
  "private": true,
  "version": "1.1.0",
  "name": "minikit-monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "build": "turbo run build",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "turbo": "^2.1.1"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.9.0"
}
